describe('ngRottenTomatoes', function() {
  it('should be a module', function() {
    var ngRottenTomatoesMdl = angular.module('ngRottenTomatoes');
    expect(ngRottenTomatoesMdl).to.be.ok;
  });
});
